spring:
  profiles:
    default: local
  datasource:
    url: jdbc:mysql://localhost:3306/hyeokDB
    username: root
    password: 1541
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
server:
  port: 8087
logging:
  level:
    org.springframework.cache: trace

